# Power Profile Report Generator - Environment Configuration
# ============================================================
#
# This file contains template environment variables for the project.
# Copy this file to .env and fill in your actual values.
#
# Quick Setup:
#   1. Copy this file: cp .env.example .env
#   2. Get API key from: https://console.anthropic.com/
#   3. Replace 'your_anthropic_api_key_here' with your actual key
#   4. Save the file
#
# IMPORTANT: Never commit your .env file to version control!

# -----------------------------------------------------------------------------
# Claude API Configuration (REQUIRED for LLM Analysis)
# -----------------------------------------------------------------------------
#
# The Anthropic API key is required for generating narrative analysis
# of power profile tests using Claude AI.
#
# How to get your API key:
#   1. Visit https://console.anthropic.com/
#   2. Sign up or log in to your account
#   3. Navigate to "API Keys" section
#   4. Create a new API key or copy an existing one
#   5. Paste it below (format starts with: sk-ant-...)
#
# Cost information:
#   - Claude Sonnet 4 (default): ~$3/million input tokens, $15/million output tokens
#   - Each analysis: ~14,000 input tokens + ~500 output tokens
#   - Estimated cost per analysis: $0.05-0.10
#
# For more information: https://docs.anthropic.com/
#
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# -----------------------------------------------------------------------------
# Logging Configuration (Optional)
# -----------------------------------------------------------------------------
#
# Controls the verbosity of log output
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
#
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Advanced Claude API Settings (Optional - Use Defaults if Unsure)
# -----------------------------------------------------------------------------
#
# Uncomment and modify these only if you need custom configuration

# Claude model to use (default: claude-sonnet-4-20250514)
# Other options: claude-3-5-sonnet-20241022, claude-3-opus-20240229
# CLAUDE_MODEL=claude-sonnet-4-20250514

# API request timeout in seconds (default: 60)
# Increase if experiencing timeout errors on slow connections
# CLAUDE_TIMEOUT=60

# Maximum tokens for API response (default: 2000)
# Adjust for longer/shorter narratives
# CLAUDE_MAX_TOKENS=2000

# API base URL (default: https://api.anthropic.com)
# Only change if using a proxy or enterprise endpoint
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# -----------------------------------------------------------------------------
# Development & Testing (Optional)
# -----------------------------------------------------------------------------

# Enable verbose debug logging (default: false)
# DEBUG=false

# Skip actual API calls in tests, use mocks instead (default: true)
# TEST_MOCK_API=true

# -----------------------------------------------------------------------------
# Security Best Practices
# -----------------------------------------------------------------------------
#
# âœ… DO:
#   â€¢ Keep your .env file private and secure
#   â€¢ Use different API keys for development and production
#   â€¢ Rotate keys regularly
#   â€¢ Monitor API usage in Anthropic Console
#   â€¢ Set spending limits in your Anthropic account
#
# âŒ DON''T:
#   â€¢ Commit .env file to git (it''s in .gitignore)
#   â€¢ Share your API keys publicly or in screenshots
#   â€¢ Hard-code API keys in source code
#   â€¢ Use production keys for testing/development
#
# If you accidentally expose your API key:
#   1. Immediately revoke it in the Anthropic Console
#   2. Generate a new key
#   3. Update your .env file
#   4. Check git history for accidental commits (git log -p | grep -i "api")
#   5. Consider rotating any other potentially exposed secrets
#
# -----------------------------------------------------------------------------
